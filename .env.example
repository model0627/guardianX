# =============================================================================
# 환경 변수 예시 파일
# 이 파일을 복사하여 .env.local로 이름을 변경하고 실제 값을 입력하세요
# =============================================================================

# 애플리케이션 기본 설정
NEXT_PUBLIC_APP_NAME=GuardianX
NEXT_PUBLIC_APP_VERSION=1.0.0
NODE_ENV=development

# =============================================================================
# PostgreSQL 데이터베이스 설정
# =============================================================================

# 방법 1: DATABASE_URL 사용 (권장)
# 형식: postgresql://username:password@hostname:port/database_name
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/guardianx_dev

# 방법 2: 개별 설정 사용 (DATABASE_URL이 없을 때만 사용됨)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=guardianx_dev
DB_USER=your_username
DB_PASSWORD=your_password

# 데이터베이스 연결 풀 설정
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# SSL 설정 (개발환경: false, 프로덕션: true)
DB_SSL=false

# =============================================================================
# 보안 설정
# =============================================================================

# JWT 토큰 비밀키 (반드시 강력한 랜덤 문자열로 변경)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# API 키 (필요시)
API_SECRET_KEY=your-api-secret-key

# 세션 비밀키
SESSION_SECRET=your-session-secret-key

# 애플리케이션 베이스 URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# =============================================================================
# 외부 서비스 설정 (필요시 주석 해제)
# =============================================================================

# 이메일 서비스
# EMAIL_SERVICE_API_KEY=your-email-service-key
# EMAIL_FROM=noreply@guardianx.com

# 클라우드 스토리지
# CLOUD_STORAGE_BUCKET=your-bucket-name
# CLOUD_STORAGE_API_KEY=your-storage-api-key

# Google OAuth & Sheets API
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key-minimum-32-characters

# =============================================================================
# 로깅 및 모니터링
# =============================================================================

# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=debug

# 성능 모니터링 (필요시 주석 해제)
# SENTRY_DSN=your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-newrelic-key

# =============================================================================
# 개발 도구 설정
# =============================================================================

# API 문서화
SWAGGER_ENABLED=true

# 개발 모드 디버깅
DEBUG_MODE=true